---
name: Test released ccs-build action

on:
  workflow_dispatch:
  
jobs:
  test_ccs_build:
    runs-on: ubuntu-latest
    name: Test CCS Build
    steps:
      - name: Checkout
        uses: actions/checkout@v3.5.3
      
      - name: 'Login to github container registry'
        uses: docker/login-action@v2.2.0
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: CCS Build action step
        # uses: apollo-fire/ccs-build@v0.0.5
        uses: apollo-fire/ccs-build@release/abc-63-build-server-add-release-artefact 
        with:
          project-path: 'test/working_ccs_project'
          project-name: 'msp430_test_project'
          build-configuration: Release

